services:
  - type: web
    name: b2b-diamond-voice-agent
    runtime: node
    buildCommand: yarn install && yarn build
    startCommand: yarn start
    envVars:
      - key: NODE_OPTIONS
        value: --max-old-space-size=400
      - key: WEB_CONCURRENCY
        value: 1
